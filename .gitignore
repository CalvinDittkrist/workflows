__pycache__/
*.pyc
.DS_Store
.claude/settings.local.json
factory/factory
factory/factory.json
factory/data/
# The binaries `make binaries` builds; a release carries them, the repository does not.
/dist/
