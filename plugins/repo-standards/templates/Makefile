# The gate: `make check` runs everything CI gates on, locally and in the CI job named `check`.
.PHONY: check
check:
	@echo 'error: the check target is a placeholder; <fill in> the lint, test and build commands CI gates on' >&2; exit 1
